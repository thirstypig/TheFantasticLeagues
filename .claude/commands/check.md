Quick health check — run in parallel:

1. `npm run test` — all tests
2. `cd client && npx tsc --noEmit` — client TypeScript
3. `cd server && npx tsc --noEmit` — server TypeScript

Report: total tests passing, any failures, any type errors. Keep it brief.
